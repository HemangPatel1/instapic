<%= form_for @pic, html: { multipart: true } do |f| %>
	<p>
		<%= f.label :caption %><br>
		<%= f.text_field :caption %><br>
		<%= f.label :image %><br>
		<%= f.file_field :image %>
	</p>
	<p>
		<%= f.submit "Share" %>
	</p>
<% end %>